Logger : change writeJobActivation : shouldn't take a list.

Tasks (every task) : delete getNextJob / getNextArrivalTime => TimeTrigged dies.
                     rename getRealNextJob.

Doesn't work with sporadic.

Check if the result is good.